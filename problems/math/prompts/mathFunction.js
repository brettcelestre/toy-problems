/*
Write a function which accepts various number of arguments and
does math calculations in the way below:
  // mathFunc(a,b)('+'); // >> returns a+b
  // mathFunc(a,b,c,d)('*')  // >> returns a*b*c*d
*/
