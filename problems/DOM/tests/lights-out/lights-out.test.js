'use strict';

const solution = require('../prompts/app.js');
const assert = require ('chai').assert;

describe('Lights Out', () => {
  
  describe('...', () => {
    it('Should return ...', () => {
      // assert.equal(limitTwo.length, 9, 'Length should equal 9');
    });
  });
  

});
